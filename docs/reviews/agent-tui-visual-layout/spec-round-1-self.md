severity: MAJOR  
location: `docs/superpowers/specs/2026-08-04-agent-tui-visual-layout-design.md:62-90, 99-107`  
evidence: The 60x10 acceptance criterion requires the selected suggestion, one-row editor, notice, and footer to remain visible, while the design also requires an outer frame and labeled persistent regions. The spec provides no row-allocation table or priority/degradation rules for header, summary, transcript, section labels/borders, initial example text, notices, and footer at that height. It also gives no compact-layout contracts for palette, pending, recovery, locked ambiguous-completion, or temporary views. This is material because the current model dynamically renders all of those states (`cmd/tu-tien-cli/tui.go:449-470, 573-603`), and the existing Bubbles list, textarea, viewport, and help models can each add variable rendered rows.  
required change: Define an explicit row budget and deterministic omission/compaction order for 60x18 and 60x10, including textarea’s 1–3-row behavior and a guaranteed minimum viewport height. Specify complete rendering and keyboard-help contracts for palette, pending, recovery, locked, unseen, and each temporary view at both sizes; state which content may be hidden or moved into the viewport. Add layout tests for each state at both target dimensions, asserting total rendered height and required visible controls.

severity: MAJOR  
location: `docs/superpowers/specs/2026-08-04-agent-tui-visual-layout-design.md:35-36, 56-60, 64-65`  
evidence: The proposal requires exactly three independently accessible suggestion rows at 60x18 and one selected suggestion at compact heights, but it does not define how the existing unbounded `suggestions []string` state is constrained or navigated. Current result handling copies every `TurnResult.SuggestedActions` item (`cmd/tu-tien-cli/tui.go:442`), rendering emits every item (`cmd/tu-tien-cli/tui.go:620-637`), numeric selection accepts every index (`cmd/tu-tien-cli/tui.go:348-353`), and Tab cycles every item (`cmd/tu-tien-cli/tui.go:329-337`). The spec’s “if the product later provides more than three” rule does not resolve the current result type’s already-unbounded input or ensure that an off-screen selected suggestion remains discoverable and operable.  
required change: Define the authoritative suggestion-limit behavior at result ingestion and interaction time: either cap the stored/UI-selectable list to three, or define accessible paging/selection that keeps the selected item visible and makes the total/range explicit. Specify matching numeric-input and Tab behavior in normal and compact modes, and add tests with more than three suggestions plus long UTF-8 suggestion text at 60x18 and 60x10.

VERDICT: NEEDS_CHANGES
